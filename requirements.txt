# ============================================================================
# FUNCTION APP DEPENDENCIES (requirements.txt)
# ============================================================================
# EPOCH: 5 - DAG ORCHESTRATION
# PURPOSE: Dependencies for Azure Function App deployment
# NOTE: This is the file Azure Functions reads during remote build.
#       Docker images use requirements-orchestrator.txt / requirements-worker.txt.
# CREATED: 04 FEB 2026
# ============================================================================

# Azure Functions Runtime
azure-functions>=1.17.0

# Service Bus Messaging (gateway_bp generic submit)
azure-servicebus>=7.11.0
azure-identity>=1.15.0

# Database Access (read-only queries)
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# Schema Validation (Pydantic V2)
pydantic>=2.5.0

# HTTP Client (proxy mutations to orchestrator)
httpx>=0.26.0
