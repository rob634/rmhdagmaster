# ============================================================================
# DAG Orchestrator/UI Dependencies
# ============================================================================
# Lightweight container for orchestration and admin UI.
# NO GDAL, NO geo libraries - those live in the worker container.
#
# Base image: python:3.12-slim-bookworm (~150MB)
# Final image: ~250MB
#
# CREATED: 29 JAN 2026
# ============================================================================

# Azure - Core
azure-identity>=1.15.0
azure-storage-blob>=12.19.0
azure-servicebus>=7.11.0
azure-monitor-opentelemetry>=1.6.0
aiohttp>=3.9.0  # Required for async Azure identity

# Database - Shared Postgres repositories (psycopg3 with C speedups)
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0
asyncpg>=0.29.0

# Web/UI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
httpx>=0.27.0

# Schema validation
pydantic>=2.0.0
pydantic-settings>=2.1.0

# Health monitoring
psutil>=5.9.0
